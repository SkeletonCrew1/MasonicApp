apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: application
spec:
  type: ClusterIP
  ports:
    - targetPort: 5173
      port: 5173
  selector:
    microservice: frontend